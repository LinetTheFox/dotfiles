# Shows current public IP address (not the one in local network, 
# but what the websites see)
alias myip="curl ipinfo.io/ip"

# Most archiving tools have un... command, but not tar :)
alias untar="tar -zsvf"

# Get SHA-256 checksum
alias sha="shasum -a 256"


# Short commands for vim and neovim

# This one is a simpler version as a quick text editor
alias v="nvim -u ${HOME}/.vimrc"

# This is a more ide-looking-like one
alias nv="nvim"


# Quicker clear :)
alias c="clear"

# ls, with the human-readable size and sorted by size
alias lt="ls --human-readable --size -1 -S --classify"

alias tsync="doas ntpdate -b -u 0.gentoo.pool.ntp.org"

# Various git aliases
alias gch="git checkout"
alias gl="git log --all"
alias glo="git log --oneline --all"
alias glg="git log --oneline --all --graph"
alias ga="git add --all"
alias gca="git add --all && git commit -m"
alias gp="git pull origin"
alias gpb='git pull origin `git branch --show-current`'
alias gP='git push origin `git branch --show-current`'
alias gr="git reset --hard"
alias gfa="git fetch --all"
alias gm="git merge"
alias gb="git branch"
alias gbd="git branch -D"
alias gst="git status"

# Change message of last commit
alias gcam="git commit --amend -m"

# Idk how to actually get proper one. Usually it's a device that appears in
# 'lsblk' list when you plug the flash drive or smth else. My system has an
# NVMe drive, and hence the actual device is called /dev/nvme0n1, so the new
# drive is recognized as /dev/sda. You'll have to figure this one for yourself
# if you're gonna use it. Besides bear in mind that it doesn't handle the case
# when you have multiple plugged pen drives, so only rely on it if you're like
# me - someone who barely ever uses those :)
DEFAULT_MOUNT_BLOCK_DEVICE="/dev/sda1"

alias mfd="sudo mount $DEFAULT_MOUNT_BLOCK_DEVICE /mnt/external -o umask=000,user,rw"
alias ufd="sudo umount $DEFAULT_MOUNT_BLOCK_DEVICE"
